apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: istiod
  namespace: {{ .Release.Namespace }}
spec:
  ingress:
    - {}
  podSelector:
    matchLabels:
      app: istiod
      istio: pilot
  policyTypes:
    - Ingress
