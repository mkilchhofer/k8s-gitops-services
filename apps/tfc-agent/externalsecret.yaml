apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: tfc-agent
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: akeyless-k3s-secrets

  target:
    name: tfc-agent

  data:
    - secretKey: TFC_AGENT_TOKEN
      remoteRef:
        key: /k3s/tfc-agent/agent-token
