apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: loki-to-s3
spec:
  podSelector:
    matchLabels:
      app: loki
  policyTypes:
    - Egress
  egress:
    - ports:
      - port: 443
        protocol: TCP
