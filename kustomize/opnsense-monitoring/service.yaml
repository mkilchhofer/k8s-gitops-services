apiVersion: v1
kind: Service
metadata:
  name: opnsense-monitoring
  labels:
    app.kubernetes.io/name: opnsense-monitoring
spec:
  ports:
  - name: metrics
    protocol: TCP
    port: 9100
    targetPort: 9100
  selector: {}
