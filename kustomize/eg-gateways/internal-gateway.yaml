apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: eg-internal
spec:
  gatewayClassName: eg
  infrastructure:
    parametersRef:
      group: gateway.envoyproxy.io
      kind: EnvoyProxy
      name: eg-internal-config
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: All

    - name: https
      port: 443
      protocol: HTTPS
      hostname: "*.tools.kilchhofer.info"
      tls:
        certificateRefs:
        - group: ""
          kind: Secret
          name: wildcard-tools-kilchhofer-info
        mode: Terminate
      allowedRoutes:
        namespaces:
          from: All
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-tools-kilchhofer-info
spec:
  dnsNames:
  - "*.tools.kilchhofer.info"
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: letsencrypt
  secretName: wildcard-tools-kilchhofer-info
  usages:
  - digital signature
  - key encipherment
