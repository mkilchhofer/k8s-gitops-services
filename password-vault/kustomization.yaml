apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: password-vault
commonLabels:
  app: bitwarden
resources:
  - configmap.yaml
  - ingress.yaml
  - namespace.yaml
  - service.yaml
  - statefulset.yaml
  - sealedsecret.yaml
